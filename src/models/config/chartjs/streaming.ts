export const streamingConfig = {
    x: {min: 'original', max: 'original', minRange: 3600 * 1000, minDuration: 3600 * 1000, maxDuration: (60 * (60 * 1000)), maxDelay: 4000, minDelay: 0},
    duration: (60 * (60 * 1000)),
    ttl: (60 * (60 * 1000)),
    refresh: 1000,
    delay: 2000,
    pause: false
};